{
  "tools": [
    {
      "bin": "curl",
      "required": false,
      "why": "Carries the HTTP requests for the thesaurus lookup provider. Usually already present.",
      "pkg": {
        "apt": "curl",
        "dnf": "curl",
        "pacman": "curl",
        "zypper": "curl",
        "apk": "curl",
        "brew": "curl",
        "winget": "cURL.cURL",
        "scoop": "curl",
        "choco": "curl"
      }
    },
    {
      "bin": "node",
      "required": false,
      "why": "Keeps the persistent cspell sidecar alive (`node/cspell_server.js`), which makes buffer and live spell checks fast and code-aware. Only useful alongside cspell itself: without node the one-shot cspell provider still works, it just pays process startup on every check.",
      "pkg": {
        "apt": "nodejs",
        "dnf": "nodejs",
        "pacman": "nodejs",
        "zypper": "nodejs",
        "apk": "nodejs",
        "brew": "node",
        "winget": "OpenJS.NodeJS",
        "scoop": "nodejs",
        "choco": "nodejs"
      }
    }
  ]
}
